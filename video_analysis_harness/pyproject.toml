[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "huazhongdian-video-analysis-harness"
version = "0.1.0"
description = "Unified orchestration harness for video understanding supplements and knowledge navigation."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
